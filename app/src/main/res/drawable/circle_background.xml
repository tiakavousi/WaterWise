<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" /> <!-- Default color -->
    <stroke
        android:width="4dp"
        android:color="@android:color/holo_blue_light" /> <!-- Circle outline -->
</shape>